# 🚀 PWA Улучшения - МИБС

## ✅ Что было добавлено

### 1. **Профессиональный Service Worker** (`public/sw.js`)

#### Стратегии кеширования:
- **Cache First** - для статических ресурсов (изображения, шрифты, CSS, JS)
- **Network First** - для динамических данных (API, HTML страницы)
- **Умное управление кешем** - автоматическое ограничение размера

#### Функции:
- ✅ **Офлайн режим** - приложение работает без интернета
- ✅ **Background Sync** - синхронизация данных при появлении соединения
- ✅ **Push-уведомления** - поддержка web push notifications
- ✅ **Периодическая синхронизация** - автоматическое обновление новостей в фоне
- ✅ **Мониторинг сети** - отслеживание статуса подключения

### 2. **Новые компоненты**

#### NetworkStatus (`src/components/NetworkStatus.tsx`)
- Показывает баннер при потере/восстановлении интернета
- Автоматические toast-уведомления
- Слушает события от Service Worker

#### PWAManager (`src/components/PWAManager.tsx`)
- Управление обновлениями Service Worker
- Мониторинг размера кеша
- Очистка кеша
- Обновление кеша

#### PushNotifications (`src/components/PushNotifications.tsx`)
- Запрос разрешений на уведомления
- Подписка/отписка от push-уведомлений
- Управление подписками

#### PWAStatus (`src/components/PWAStatus.tsx`)
- Индикатор статуса подключения (Онлайн/Офлайн)
- Индикатор установки PWA
- Отображение размера кеша
- Расположен в правом верхнем углу

#### LoadingScreen (`src/components/LoadingScreen.tsx`)
- Красивый экран загрузки с прогресс-баром
- Анимация логотипа
- Показывается при первой загрузке

### 3. **Хуки**

#### useOfflineStorage (`src/hooks/useOfflineStorage.ts`)
```typescript
const { 
  isOnline,           // Статус подключения
  pendingData,        // Несохраненные данные
  hasPendingData,     // Есть ли данные для синхронизации
  saveForLater,       // Сохранить данные для отправки позже
  syncPendingData,    // Синхронизировать сохраненные данные
  clearPendingData    // Очистить сохраненные данные
} = useOfflineStorage();
```

#### usePWA (из PWAManager.tsx)
```typescript
const { 
  isInstalled,  // Установлено ли приложение
  isOnline,     // Есть ли подключение
  canInstall    // Можно ли установить
} = usePWA();
```

#### usePushNotifications (из PushNotifications.tsx)
```typescript
const { 
  permission,          // Статус разрешений
  isSubscribed,        // Подписан ли пользователь
  canNotify,           // Можно ли отправлять уведомления
  requestPermission    // Запросить разрешение
} = usePushNotifications();
```

### 4. **Улучшения главной страницы**

- ✨ Плавные анимации появления элементов
- 🎨 Анимированные декоративные круги
- 📱 PWA статус индикатор
- ⚡ Экран загрузки при первом открытии
- 🎯 Улучшенные hover-эффекты
- 💫 Backdrop blur эффекты

---

## 📊 Версии Service Worker

**Текущая версия:** `mibs-v2.0.0`

### Кеши:
- `mibs-v2.0.0-static` - Статические ресурсы (HTML, JS, CSS, шрифты)
- `mibs-v2.0.0-dynamic` - Динамические данные (API ответы)
- `mibs-v2.0.0-images` - Изображения

---

## 🎯 Как использовать

### Проверка статуса PWA
```typescript
import { usePWA } from '@/components/PWAManager';

function MyComponent() {
  const { isInstalled, isOnline, canInstall } = usePWA();
  
  return (
    <div>
      {isInstalled ? '✅ Установлено' : '❌ Не установлено'}
      {isOnline ? '🌐 Онлайн' : '📡 Офлайн'}
    </div>
  );
}
```

### Офлайн хранилище
```typescript
import { useOfflineStorage } from '@/hooks/useOfflineStorage';

function RegistrationForm() {
  const { isOnline, saveForLater, syncPendingData } = useOfflineStorage();
  
  const handleSubmit = async (data) => {
    if (!isOnline) {
      // Сохраняем для отправки позже
      saveForLater(data);
      toast.info('Данные сохранены и будут отправлены при подключении');
    } else {
      // Отправляем сразу
      await fetch('/api/registrations', {
        method: 'POST',
        body: JSON.stringify(data)
      });
    }
  };
}
```

### Push-уведомления
```typescript
import { usePushNotifications } from '@/components/PushNotifications';

function NotificationSettings() {
  const { permission, requestPermission, isSubscribed } = usePushNotifications();
  
  return (
    <button onClick={requestPermission}>
      {permission === 'granted' ? '🔔 Уведомления включены' : '🔕 Включить уведомления'}
    </button>
  );
}
```

---

## 🔧 Service Worker API

### Отправка сообщений Service Worker
```typescript
// Очистить кеш
if (navigator.serviceWorker.controller) {
  const messageChannel = new MessageChannel();
  
  messageChannel.port1.onmessage = (event) => {
    if (event.data.success) {
      console.log('Кеш очищен!');
      window.location.reload();
    }
  };
  
  navigator.serviceWorker.controller.postMessage(
    { type: 'CLEAR_CACHE' },
    [messageChannel.port2]
  );
}

// Получить размер кеша
if (navigator.serviceWorker.controller) {
  const messageChannel = new MessageChannel();
  
  messageChannel.port1.onmessage = (event) => {
    console.log('Размер кеша:', event.data.size);
  };
  
  navigator.serviceWorker.controller.postMessage(
    { type: 'GET_CACHE_SIZE' },
    [messageChannel.port2]
  );
}

// Обновить кеш
if (navigator.serviceWorker.controller) {
  navigator.serviceWorker.controller.postMessage(
    { type: 'UPDATE_CACHE' }
  );
}

// Пропустить ожидание (применить обновление немедленно)
if (navigator.serviceWorker.controller) {
  navigator.serviceWorker.controller.postMessage(
    { type: 'SKIP_WAITING' }
  );
}
```

---

## 🎨 UI Индикаторы

### PWA Status (правый верхний угол)
- **🟢 Онлайн** - подключение к интернету есть
- **🔴 Офлайн** - подключения нет
- **📱 PWA** - приложение установлено
- **💾 [число]** - количество кешированных элементов

### Network Status Banner
- Появляется при потере/восстановлении соединения
- Автоматически исчезает через 3 секунды
- Красный баннер - нет подключения
- Зеленый баннер - подключение восстановлено

---

## 🔄 Background Sync

### Автоматическая синхронизация
Service Worker автоматически пытается отправить сохраненные данные когда:
- Подключение к интернету восстановлено
- Пользователь вернулся в приложение
- Произошло событие `sync`

### Поддерживаемые теги синхронизации:
- `sync-registrations` - синхронизация регистраций на курсы
- `sync-offline-data` - синхронизация других офлайн данных
- `update-news` - обновление новостей в фоне
- `check-updates` - проверка обновлений приложения

---

## 📱 Push-уведомления

### Настройка (для backend)
Для работы push-уведомлений нужно:

1. Сгенерировать VAPID ключи:
```bash
npx web-push generate-vapid-keys
```

2. Добавить в `.env`:
```env
NEXT_PUBLIC_VAPID_PUBLIC_KEY=ваш_публичный_ключ
VAPID_PRIVATE_KEY=ваш_приватный_ключ
```

3. Создать API endpoints:
- `POST /api/push/subscribe` - сохранение подписки
- `POST /api/push/unsubscribe` - удаление подписки
- `POST /api/push/send` - отправка уведомлений

### Пример отправки уведомления (backend)
```typescript
import webpush from 'web-push';

webpush.setVapidDetails(
  'mailto:your-email@example.com',
  process.env.NEXT_PUBLIC_VAPID_PUBLIC_KEY,
  process.env.VAPID_PRIVATE_KEY
);

// Отправка уведомления
await webpush.sendNotification(subscription, JSON.stringify({
  title: 'МИБС',
  body: 'Новый курс доступен для записи!',
  icon: '/icon-192.png',
  url: '/registration'
}));
```

---

## 🎯 Производительность

### Размеры кешей
- **Статический кеш**: неограничен (только важные файлы)
- **Динамический кеш**: макс 50 элементов
- **Кеш изображений**: макс 30 элементов

### Стратегии кеширования
- **Изображения**: Cache First (быстрая загрузка)
- **API запросы**: Network First (актуальные данные)
- **HTML страницы**: Network First с fallback на Offline
- **Статика (JS/CSS)**: Cache First (мгновенная загрузка)

---

## 🐛 Отладка

### Логи Service Worker
Откройте консоль браузера и найдите логи с префиксом `[SW]`:
- 🚀 Установка
- ⚡ Активация
- 💾 Кеширование
- 📡 Сеть
- 🔄 Синхронизация
- 🔔 Уведомления

### Проверка кеша
1. Chrome DevTools → Application → Cache Storage
2. Найдите кеши с префиксом `mibs-v2.0.0`
3. Просмотрите закешированные ресурсы

### Проверка Service Worker
1. Chrome DevTools → Application → Service Workers
2. Проверьте статус регистрации
3. Можно "Unregister" для тестирования установки заново

---

## 📈 Метрики

### Что отслеживается
- ✅ Размер кеша
- ✅ Количество офлайн запросов
- ✅ Статус подключения
- ✅ Статус установки PWA
- ✅ Подписки на уведомления

### Где смотреть
- PWA Status индикатор (правый верхний угол)
- Консоль браузера (логи Service Worker)
- Chrome DevTools → Application

---

## 🎉 Результат

### До улучшений:
- ❌ Базовое кеширование
- ❌ Нет офлайн режима
- ❌ Нет уведомлений
- ❌ Нет индикаторов статуса

### После улучшений:
- ✅ Профессиональное кеширование (3 уровня)
- ✅ Полный офлайн режим с синхронизацией
- ✅ Push-уведомления
- ✅ Background sync
- ✅ Индикаторы статуса (PWA, сеть, кеш)
- ✅ Автоматическое обновление
- ✅ Красивые анимации
- ✅ Экран загрузки

---

## 🚀 Дальнейшие улучшения (опционально)

1. **IndexedDB** - для хранения больших объемов данных
2. **Web Share API** - делиться курсами
3. **Геолокация** - определение местоположения
4. **Camera API** - QR-код сканер
5. **Periodic Background Sync** - регулярное обновление данных
6. **Web Bluetooth** - подключение устройств
7. **Badge API** - счетчик на иконке приложения

---

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи в консоли
2. Проверьте регистрацию Service Worker
3. Очистите кеш и попробуйте снова
4. Убедитесь, что используется HTTPS (или localhost)

**Версия PWA:** 2.0.0  
**Service Worker:** mibs-v2.0.0  
**Дата обновления:** 2024-01-27
